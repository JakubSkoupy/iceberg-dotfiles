

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# smart_borders on
# smart_gaps on

# COLORS START
set $highlight_accent_0 #508BBF
set $highlight_accent_1 #4EABBF

set $dimmed_0 #0A0C0D
set $dimmed_1 #212426
set $dimmed_2 #333A40
set $dimmed_3 #465259
set $dimmed_4 #475759

set $deep_accent_0 #161E26
set $deep_accent_1 #253141
set $deep_accent_2 #2E4059

set $mild_0 #262A25
set $mild_1 #56778C
set $mild_2 #8FADBF
set $mild_3 #F2ACC6
set $mild_4 #EAD9C6

set $red_0 #E52535
set $red_1 #FF293B

set $yellow_0 #F2B630
set $yellow_1 #F2C752

set $green_0 #4EAE4E
set $green_1 #6AEB6A

set $cyan_0 #00E3CC
set $cyan_1 #44E3D3

set $blue_0 #00547F
set $blue_1 #0087CC

set $purple_0 #9C27B0
set $purple_1 #A82ABD

set $white #FFFFFF
# COLORS END

default_border pixel 2
default_floating_border pixel 10


gaps inner 12
gaps outer 6

# TEMPLATE::            TAB BORDER              TAB BACKGROUND          TAB TITLE
#                       SPLIT BORDER            WINDOW BORDER
                        

client.focused          $highlight_accent_1     $highlight_accent_1     $dimmed_0\
                        $mild_2                 $highlight_accent_1

client.focused_inactive $dimmed_3               $dimmed_3     $white
                        $dimmed_3               $dimmed_3

client.unfocused        $dimmed_3               $dimmed_2     $dimmed_0
                        $dimmed_2               $dimmed_1

client.urgent           $highlight_accent_1     $red_1                  $white
                        $dimmed_3               $dimmed_3

client.placeholder      $highlight_accent_1     $red_1     $white
                        $dimmed_3               $dimmed_3


set $mod Mod1
focus_follows_mouse no

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:RobotoMonoNerdFont 0

bindsym $mod+equal exec --no-startup-id setxkbmap us
bindsym $mod+minus exec --no-startup-id setxkbmap "cz(qwerty)"

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3
exec nitrogen --restore &
exec ulauncher
exec picom &
exec ~/.screenlayout/default.sh
exec_always kmonad /home/kuba/clones/kmonad/keymap/iceberg_01.kbd &
exec_always --no-startup-id xset r rate 180 35
exec_always --no-startup-id ~/.config/startup.sh &

exec --no-startup-id i3-msg workspace $ws2; move workspace to output HDMI-1
exec --no-startup-id i3-msg workspace $ws1

exec --no-startup-id firefox &
exec --no-startup-id kitty --name dotfiles -e nvim ~/.dotfiles &
exec --no-startup-id rm -rf "~/.dotfiles/iceberg/workspaces/minimized"
exec_always --no-startup-id i3-layouts
for_window [instance="nvim_config"] move workspace config_nvim 
for_window [instance="dotfiles"] move workspace dotfiles



exec_always --no-startup-id ~/.dotfiles/iceberg/session/build.sh
bindsym $mod+b exec --no-startup-id ~/.dotfiles/iceberg/session/build.sh

# tHe combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym $mod+m exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym $mod+n exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym $mod+Shift+m exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1% && $refresh_i3status
bindsym $mod+Shift+n exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym $mod+Shift+s exec maim -s | xclip -selection clipboard -t image/png

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym End exec --no-startup-id dmenu_run
bindsym End exec --no-startup-id rofi -show drun
bindsym Mod4+q exec --no-startup-id rofi -show run
bindsym Mod4+w exec --no-startup-id rofi -show window
bindsym $mod+q exec --no-startup-id /home/kuba/.config/rofi/workspaces_move.sh
bindsym $mod+w exec --no-startup-id /home/kuba/.config/rofi/workspaces.sh
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

bindsym $mod+1 [instance="quick"] scratchpad show; move position center
bindsym $mod+2 [instance="fileman"] scratchpad show; move position center
bindsym $mod+3 [instance="spotify"] scratchpad show; move position center
bindsym $mod+4 [instance="python_interpret"] scratchpad show; move position center
bindsym $mod+5 [instance="pulse"] scratchpad show; move position center
bindsym $mod+6 [instance="btop"] scratchpad show; move position center

# RANGER SCRATCHPAD

exec --no-startup-id kitty --name fileman -e ranger
for_window [instance="fileman"] floating enable
for_window [instance="fileman"] resize set 1280 800
for_window [instance="fileman"] move position center
for_window [instance="fileman"] move scratchpad

# SPOTIFY SCRATCHPAD
exec --no-startup-id spotify
for_window [instance="spotify"] floating enable
for_window [instance="spotify"] resize set 1280 800
for_window [instance="spotify"] move position center
for_window [instance="spotify"] move scratchpad

# QUICK TERM SCRATCHPAD
exec --no-startup-id kitty --name quick
for_window [instance="quick"] floating enable
for_window [instance="quick"] resize set 1000 800
for_window [instance="quick"] move position center
for_window [instance="quick"] move scratchpad

exec --no-startup-id kitty --name python_interpret -e python3
for_window [instance="python_interpret"] floating enable
for_window [instance="python_interpret"] resize set 1000 800
for_window [instance="python_interpret"] move position center
for_window [instance="python_interpret"] move scratchpad

exec --no-startup-id kitty --name pulse -e pulsemixer
for_window [instance="pulse"] floating enable
for_window [instance="pulse"] resize set 800 600
for_window [instance="pulse"] move position center
for_window [instance="pulse"] move scratchpad

exec --no-startup-id kitty --name btop -e htop 
for_window [instance="btop"] floating enable
for_window [instance="btop"] resize set 1280 800
for_window [instance="btop"] move position center
for_window [instance="btop"] move scratchpad



for_window [class="steam"] border pixel 2

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Ctrl+j move left
bindsym $mod+Ctrl+k move down
bindsym $mod+Ctrl+l move up
bindsym $mod+Ctrl+semicolon move right

bindsym $mod+Shift+j exec i3l move left
bindsym $mod+Shift+k exec i3l move down
bindsym $mod+Shift+l exec i3l move up
bindsym $mod+Shift+semicolon exec i3l move right

bindsym $mod+i resize shrink width 1 px or 1 ppt
bindsym $mod+Shift+i resize shrink height 1 px or 1 ppt
bindsym $mod+Shift+o resize grow height 1 px or 1 ppt
bindsym $mod+o resize grow width 1 px or 1 ppt

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+f exec --no-startup-id bash /home/kuba/.config/i3/scripts/toggle_smart.sh $

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+Shift+z layout stacked
bindsym $mod+Shift+x layout toggle split

# toggle tiling / fgit clone https://github.com/jdholtz/i3-restore.git
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent
bindsym Mod4+Shift+j move workspace to output right
bindsym Mod4+Shift+semicolon move workspace to output left

bindsym $mod+a focus output left
bindsym $mod+s focus output right

# focus the child container
#bindsym $mod+d focus child
# Define names for default workspaces for which we configure key bindings later on.
# We use variables  to avoid repeating the names in multiple places.
set $x_switch exec --no-startup-id python3 /home/kuba/.config/i3/i3-wk-switch/i3-wk-switch.py
#
set $ws1 "main"
set $ws2 "info"
set $ws3 "docs"
set $ws4 "term"
set $ws5 "brws"
set $ws6 "media"
set $ws7 "brwf"
set $ws8 "music"
set $ws9 "is"
set $ws10 "sys"
set $ws11 "bg"
set $ws12 "files"
set $ws13 "design"
set $ws14 "vid"
set $ws15 "obs"
set $ws16 "steam"
#
# # switch to workspace
# bindsym $mod+1 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 1"
# bindsym $mod+2 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 2"
# bindsym $mod+3 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 3"
# bindsym $mod+4 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 4"
# bindsym $mod+5 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 5"
# bindsym $mod+6 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 6"
# bindsym $mod+7 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 7"
# bindsym $mod+8 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 8"
# bindsym $mod+9 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 9"
# bindsym $mod+0 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/switch_workspace.sh 10"
#
bindsym $mod+Shift+1 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 1"
bindsym $mod+Shift+2 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 2"
bindsym $mod+Shift+3 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 3"
bindsym $mod+Shift+4 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 4"
bindsym $mod+Shift+5 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 5"
bindsym $mod+Shift+6 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 6"
bindsym $mod+Shift+7 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 7"
bindsym $mod+Shift+8 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 8"
bindsym $mod+Shift+9 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 9"
bindsym $mod+Shift+0 exec --no-startup-id "/home/kuba/.dotfiles/polybar/scripts/workspaces/move_to_workspace.sh 10"
#

bindsym $mod+e exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/minimize.sh"
bindsym Mod4+1 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 1"
bindsym Mod4+2 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 2"
bindsym Mod4+3 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 3"
bindsym Mod4+4 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 4"
bindsym Mod4+5 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 5"
bindsym Mod4+6 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 6"
bindsym Mod4+7 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 7"
bindsym Mod4+8 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 8"
bindsym Mod4+9 exec --no-startup-id "bash ~/.dotfiles/iceberg/workspaces/maximize.sh 9"

# # move focused container to workspace
# bindsym $mod+Shift+1 move container to workspace $ws1
# bindsym $mod+Shift+2 move container to workspace $ws2
# bindsym $mod+Shift+3 move container to workspace $ws3
# bindsym $mod+Shift+4 move container to workspace $ws4
# bindsym $mod+Shift+5 move container to workspace $ws5
# bindsym $mod+Shift+6 move container to workspace $ws6
# bindsym $mod+Shift+7 move container to workspace $ws7
# bindsym $mod+Shift+8 move container to workspace $ws8
# bindsym $mod+Shift+9 move container to workspace $ws9
# bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
set $i3l spiral to workspace $ws1
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
exec_always --no-startup-id killall -q polybar ; polybar main && polybar secondary & 
# bar {
#
#         status_command polybar secondary
# }

